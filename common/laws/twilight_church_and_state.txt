# Overwritten
law_freedom_of_conscience = {
	group = lawgroup_church_and_state

	icon = "gfx/interface/icons/law_icons/freedom_of_conscience.dds"

	progressiveness = 0

	unlocking_technologies = {
		rationalism
	}

	disallowing_laws = {
		law_theocracy
	}

	on_enact = {
		recalculate_pop_ig_support = yes
	}

	religious_acceptance_rule = {
		save_temporary_scope_as = target_religion
		religion_accepted_under_law_freedom_of_conscience = yes
	}

	modifier = {
		interest_group_ig_devout_pol_str_mult = 0.15
		state_radicals_from_sol_change_accepted_religion_mult = -0.05
		state_loyalists_from_sol_change_accepted_religion_mult = 0.05
		country_authority_add = 100
	}

	possible_political_movements = {
		law_total_separation
	}

	pop_support = {
		value = 0
		add = {
			desc = "POP_ACADEMICS"
			# Academics generally against religious perseecution
			if = {
				limit = {
					owner = {
						NOT = { has_law = law_type:law_total_separation }
					}
					is_pop_type = academics
				}
				value = 0.25
			}
		}

		add = {
			desc = "POP_DISCRIMINATED"
			if = {
				limit = {
					religion_accepted = no
					owner = { NOT = { has_law = law_type:law_total_separation } }
				}
				add = 0.5

				if = {
					limit = {
						standard_of_living <= 10
					}
					add = 0.5
				}
			}
		}
	}
}

# Overwritten
law_total_separation = {
	group = lawgroup_church_and_state

	icon = "gfx/interface/icons/law_icons/total_separation.dds"

	progressiveness = 50

	unlocking_technologies = {
		empiricism
	}

	disallowing_laws = {
		law_theocracy
	}

	on_enact = {
		recalculate_pop_ig_support = yes
	}

	religious_acceptance_rule = {
		save_temporary_scope_as = target_religion
		religion_accepted_under_law_total_separation = yes
	}

	possible_political_movements = {
		law_freedom_of_conscience
	}

	pop_support = {
		value = 0

		add = {
			desc = "POP_ACADEMICS_NON_PHILOSOPHICAL_RELIGION"
			# Academics generally against religious perseecution
			if = {
				limit = {
					owner.religion = {
						NOT = {
							has_discrimination_trait = philosophical
						}
					}
					is_pop_type = academics
				}
				value = 0.25
			}
		}

		add = {
			desc = "POP_DISCRIMINATED"
			if = {
				limit = {
					religion_accepted = no
				}
				add = 0.5

				if = {
					limit = {
						standard_of_living <= 10
					}
					add = 0.5
				}
			}
		}
	}

	# Added
	ai_will_do = {
		exists = this
		exists = ruler
		exists = ig:ig_devout
		NOT = {
			ruler.religion = religion
			ruler.interest_group = ig:ig_devout
		}
	}
}
